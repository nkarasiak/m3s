Grid Systems
============

One self-contained example per spatial grid system supported by M3S. Each
example builds cells over a small region (or a point and its neighbors for
metre-scale grids), plots them, and reports cell count and area.

All examples use the simplified API with GIS-native ``(lon, lat)`` coordinate
order.
